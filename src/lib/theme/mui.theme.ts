import { createTheme } from '@mui/material';

export const muiTheme = createTheme({
  breakpoints: {
    values: {
      xs: 320,
      sm: 425,
      md: 768,
      lg: 1200,
      xl: 2560
    }
  }
});
